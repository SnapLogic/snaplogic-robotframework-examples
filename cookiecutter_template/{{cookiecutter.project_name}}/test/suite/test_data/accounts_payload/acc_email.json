{
    "path": "{{ACCOUNT_LOCATION_PATH}}",
    "duplicate_check": true,
    "account": {
        "class_fqid": "com-snaplogic-snaps-email-smtpaccount_1-main31019",
        "property_map": {
            "view_serial": 100,
            "settings": {
                "port": {
                    "value": "{{EMAIL_PORT}}"
                },
                "writeTimeout": {
                    "value": 60000
                },
                "connectionTimeout": {
                    "value": 60000
                },
                "serverDomain": {
                    "value": "{{EMAIL_SERVER_DOMAIN}}"
                },
                "mailProperties": {
                    "value": []
                },
                "readTimeout": {
                    "value": 60000
                },
                "secureConnection": {
                    "value": "{{EMAIL_SECURE_CONNECTION}}"
                },
                "emailId": {
                    "value": "{{EMAIL_ID}}"
                },
                "password": {
                    "value": "{{EMAIL_PASSWORD}}"
                }
            },
            "info": {
                "label": {
                    "value": "{{EMAIL_ACCOUNT_NAME}}"
                },
                "notes": {
                    "value": null
                }
            }
        },
        "test_settings": {
            "runtime_path_id": "ml-legacy-migration/rt/sidekick/slgroundple2xautomationdev"
        }
    }
}