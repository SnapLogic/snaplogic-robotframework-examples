# ============================================================================
#                              PORT CONFIGURATION
# ============================================================================
# These ports can be overridden by setting environment variables
# If you're running multiple instances, change these to avoid conflicts
# Format: HOST_PORT:CONTAINER_PORT (we're configuring the HOST_PORT here)
# ============================================================================

# ============================================
# GROUNDPLEX SERVICE PORTS
# ============================================
# The ports exposed by the Groundplex container for API and JCC services

# Groundplex API port (maps to container port 8090)
GROUNDPLEX_API_PORT=8090

# Groundplex JCC port (maps to container port 8081)
GROUNDPLEX_JCC_PORT=8081

# ============================================
# DATABASE SERVICE PORTS
# ============================================
# Default ports for various database services

# PostgreSQL database (maps to container port 5432)
POSTGRES_PORT=5432

# MySQL database (maps to container port 3306)
MYSQL_PORT=3306

# Oracle database (maps to container port 1521)
ORACLE_PORT=1521

# SQL Server database (maps to container port 1433)
SQLSERVER_PORT=1433

# DB2 database (maps to container port 50000)
DB2_PORT=50000

# Teradata database (maps to container port 1025)
TERADATA_PORT=1025

# ============================================
# EMAIL MOCK SERVICE PORTS (MAILDEV)
# ============================================
# MailDev provides both SMTP server and Web UI

# SMTP server for capturing emails (maps to container port 1025)
MAILDEV_SMTP_PORT=1025

# Web UI to view captured emails (maps to container port 1080)
MAILDEV_WEB_PORT=1080

# ============================================
# S3/MINIO OBJECT STORAGE PORTS
# ============================================
# MinIO provides S3-compatible object storage

# MinIO API endpoint (maps to container port 9000)
MINIO_API_PORT=9010

# MinIO web console (maps to container port 9001)
MINIO_CONSOLE_PORT=9011

# ============================================
# SALESFORCE MOCK SERVICE PORTS
# ============================================
# WireMock and JSON Server for Salesforce API simulation

# WireMock HTTP port (maps to container port 8080)
SALESFORCE_HTTP_PORT=8089

# WireMock HTTPS port (maps to container port 8443)
SALESFORCE_HTTPS_PORT=8443

# JSON Server for persistent data (maps to container port 3000)
SALESFORCE_JSON_PORT=8082

# ============================================
# ACTIVEMQ MESSAGE BROKER PORTS
# ============================================
# ActiveMQ provides multiple protocol endpoints

# ActiveMQ web console (maps to container port 8161)
ACTIVEMQ_WEB_PORT=8161

# JMS/OpenWire protocol (maps to container port 61616)
ACTIVEMQ_JMS_PORT=61616

# STOMP protocol (maps to container port 61613)
ACTIVEMQ_STOMP_PORT=61613

# AMQP protocol (maps to container port 5672)
ACTIVEMQ_AMQP_PORT=5672

# ============================================
# KAFKA STREAMING PLATFORM PORTS
# ============================================
# Apache Kafka message broker and related services

# Kafka broker port (maps to container port 9092)
KAFKA_PORT=9092

# Zookeeper port for Kafka coordination (maps to container port 2181)
ZOOKEEPER_PORT=2181

# JMX port for Kafka monitoring (maps to container port 9101)
KAFKA_JMX_PORT=9101

# Kafka UI web console port (maps to container port 8080)
KAFKA_UI_PORT=8080

# Schema Registry port (maps to container port 8081)
KAFKA_SCHEMA_REGISTRY_PORT=8081




